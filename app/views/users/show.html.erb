<div class="contents">
  <h5 class="center"> <%= @nickname %>の投稿一覧 </h5>
  <% @tweets.each do |tweet| %>
    <%= render partial: "tweets/tweet", locals: { tweet: tweet }%>
  <% end %>
</div>
<div class="grey">
<%= paginate(@tweets) %>
</div>